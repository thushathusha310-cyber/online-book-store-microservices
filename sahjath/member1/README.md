# Member 1 services

Member 1 owns the existing `book-service` and `user-service`. Place those projects here when combining the team repository:

```text
member1/
├── book-service/
└── user-service/
```

They are not duplicated in this Member 2 deliverable so the original author and Git history remain clear.

